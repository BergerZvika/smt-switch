(set-logic ALL)
(declare-const k Int)
(declare-const i Int)
(declare-const x Int)
(assert (>> i 0))
(assert (< i k))
(assert (> x 0))
(assert (< x (- (^ 2 k) 1)))
(assert (distinct (mod (* x (^ 2 i)) (- (^ 2 k) 1)) (bv2nat ((_ repeat i) ((_ int2bv 32) x)))))
(check-sat)