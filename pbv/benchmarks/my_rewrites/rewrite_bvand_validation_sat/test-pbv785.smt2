(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s (_ BitVec k))
(declare-const t (_ BitVec k))
(assert (not (distinct (bvand s (bvshl t (bvshl (bvshl (_ bv1 k) s) (_ bv1 k)))) (bvand s (bvshl t (bvshl (bvnot s) s))))))
(check-sat)
(exit)