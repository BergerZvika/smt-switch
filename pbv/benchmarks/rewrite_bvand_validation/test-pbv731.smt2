(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s (_ BitVec k))
(declare-const t (_ BitVec k))
(assert (distinct (bvshl s (bvand t (bvshl (bvnot (_ bv1 k)) (_ bv1 k)))) (bvshl s (bvand t (bvshl t (bvshl t t))))))
(check-sat)
(exit)