(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvshl s (bvudiv s (bvadd s (int_to_pbv k 1)))) (bvshl (bvand s (int_to_pbv k 1)) s)))
(check-sat)
