(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvule s (bvadd t (bvudiv (int_to_pbv k 0) s))) (= s (int_to_pbv k 0))))
(check-sat)
