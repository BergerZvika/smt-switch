(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvmul s (bvshl (bvadd t (int_to_pbv k 1)) t)) (bvmul s (bvmul t (bvshl t t)))))
(check-sat)
