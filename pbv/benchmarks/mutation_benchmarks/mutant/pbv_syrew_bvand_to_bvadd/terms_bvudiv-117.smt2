(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvadd s (bvand t (bvudiv s s))) (bvand s (bvand t (int_to_pbv k 1)))))
(check-sat)
