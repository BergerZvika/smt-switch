(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (= s (bvurem t (bvadd s (int_to_pbv k 1)))) (= s (bvshl t (bvand s (int_to_pbv k 1))))))
(check-sat)
