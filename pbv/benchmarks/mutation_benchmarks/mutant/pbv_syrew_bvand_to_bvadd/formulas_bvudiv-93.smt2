(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (= s (bvudiv s (bvadd s (int_to_pbv k 1)))) (distinct (int_to_pbv k 0) (bvand s (int_to_pbv k 1)))))
(check-sat)
