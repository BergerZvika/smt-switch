(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct (int_to_pbv k k) (bvmul s (bvshl (int_to_pbv k 1) t))) (distinct (int_to_pbv k 0) (bvshl s t))))
(check-sat)
