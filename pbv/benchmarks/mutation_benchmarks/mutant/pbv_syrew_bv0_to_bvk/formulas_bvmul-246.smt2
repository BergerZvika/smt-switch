(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (= (int_to_pbv k k) (bvmul t (bvshl s (int_to_pbv k 1)))) (= (int_to_pbv k 0) (bvmul s (bvshl t (int_to_pbv k 1))))))
(check-sat)
