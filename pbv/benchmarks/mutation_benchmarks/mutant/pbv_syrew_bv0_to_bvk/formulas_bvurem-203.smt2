(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvurem t (bvurem t (int_to_pbv k k)))) (distinct s (int_to_pbv k 0))))
(check-sat)
