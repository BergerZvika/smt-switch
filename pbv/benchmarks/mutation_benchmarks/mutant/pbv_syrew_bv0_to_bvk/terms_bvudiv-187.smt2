(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvshl s (bvudiv t (bvudiv (int_to_pbv k k) t))) (bvand s (bvudiv (int_to_pbv k 0) t))))
(check-sat)
