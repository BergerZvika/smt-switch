(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (= s (bvudiv s (bvshl (int_to_pbv k 1) s))) (= s (bvnot (bvudiv (int_to_pbv k k) s)))))
(check-sat)
