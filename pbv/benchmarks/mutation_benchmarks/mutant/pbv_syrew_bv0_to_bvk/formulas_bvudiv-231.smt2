(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvult s (bvudiv t (bvudiv (int_to_pbv k k) s))) (distinct s (bvudiv t (bvudiv (int_to_pbv k 0) s)))))
(check-sat)
