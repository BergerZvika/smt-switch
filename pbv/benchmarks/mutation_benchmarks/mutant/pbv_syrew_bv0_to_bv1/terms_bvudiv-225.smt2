(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvshl s (bvudiv (int_to_pbv k 1) (bvudiv (int_to_pbv k 1) t))) (bvand s (bvudiv (int_to_pbv k 0) (bvlshr t (int_to_pbv k 1))))))
(check-sat)
