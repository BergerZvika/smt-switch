(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvudiv (bvlshr s (int_to_pbv k 1)) s) (bvudiv (int_to_pbv k 1) s)))
(check-sat)
