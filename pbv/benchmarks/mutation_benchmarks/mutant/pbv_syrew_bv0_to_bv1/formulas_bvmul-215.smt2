(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct (int_to_pbv k 1) (bvshl s (bvshl s s))) (distinct s (bvmul s (bvlshr s (int_to_pbv k 1))))))
(check-sat)
