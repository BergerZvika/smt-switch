(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvor (int_to_pbv k 1) (bvshl s t))) (distinct s (bvmul s (bvshl s t)))))
(check-sat)
