(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvor s (bvmul t (bvshl (int_to_pbv k 1) t))) (bvand s (bvshl t t))))
(check-sat)
