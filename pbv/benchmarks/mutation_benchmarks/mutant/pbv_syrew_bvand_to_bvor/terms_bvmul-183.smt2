(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvor (int_to_pbv k 1) (bvmul s (bvand s t))) (bvand s (bvand t (int_to_pbv k 1)))))
(check-sat)
