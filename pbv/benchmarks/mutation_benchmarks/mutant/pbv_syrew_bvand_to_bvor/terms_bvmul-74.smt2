(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvmul s (bvor (int_to_pbv k 1) (bvnot t))) (bvlshr s (bvmul s (bvand t (int_to_pbv k 1))))))
(check-sat)
