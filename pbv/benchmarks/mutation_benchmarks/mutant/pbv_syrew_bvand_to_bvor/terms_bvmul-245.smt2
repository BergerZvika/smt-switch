(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvmul (bvor s t) (bvand t (int_to_pbv k 1))) (bvand s (bvmul t (bvand t (int_to_pbv k 1))))))
(check-sat)
