(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvshl s (bvor t (bvudiv t t))) (bvshl s (bvand t (int_to_pbv k 1)))))
(check-sat)
