(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvult s (bvshl (bvmul s t) (int_to_pbv k 0))) (bvult s (bvmul s (bvshl t (int_to_pbv k 1))))))
(check-sat)
