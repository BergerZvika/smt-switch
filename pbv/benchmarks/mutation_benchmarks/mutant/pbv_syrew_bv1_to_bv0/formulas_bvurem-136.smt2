(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvand s (bvurem (int_to_pbv k 0) t))) (bvult (bvurem (int_to_pbv k 1) t) s)))
(check-sat)
