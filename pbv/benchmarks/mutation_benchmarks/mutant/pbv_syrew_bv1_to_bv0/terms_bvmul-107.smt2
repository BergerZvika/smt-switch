(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvmul s (bvshl (bvand s (int_to_pbv k 0)) (int_to_pbv k 1))) (bvmul s (bvmul s (bvshl s (int_to_pbv k 1))))))
(check-sat)
