(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvmul s (bvlshr (int_to_pbv k 0) t))) (distinct s (bvshl s t))))
(check-sat)
