(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct (int_to_pbv k 0) (bvadd s (bvnot s))) (distinct s (bvshl s (bvnot s)))))
(check-sat)
