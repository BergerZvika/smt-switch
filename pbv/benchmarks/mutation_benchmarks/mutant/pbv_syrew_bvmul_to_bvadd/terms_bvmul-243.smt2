(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvadd (bvand s t) (bvand s (int_to_pbv k 1))) (bvand s (bvmul t (bvand s (int_to_pbv k 1))))))
(check-sat)
