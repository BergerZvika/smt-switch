(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvsub s (bvnot (bvudiv t t))) (bvand s (bvudiv (int_to_pbv k 0) t))))
(check-sat)
