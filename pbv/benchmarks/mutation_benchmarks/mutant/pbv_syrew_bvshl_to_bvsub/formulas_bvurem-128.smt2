(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (= s (bvsub s (bvurem (int_to_pbv k 1) s))) (bvule s (int_to_pbv k 1))))
(check-sat)
