(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvsub t (bvurem (int_to_pbv k 0) t))) (distinct s t)))
(check-sat)
