(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvmul s (bvsub (bvand t (int_to_pbv k 1)) t)) (bvmul s (bvmul t (bvshl t t)))))
(check-sat)
