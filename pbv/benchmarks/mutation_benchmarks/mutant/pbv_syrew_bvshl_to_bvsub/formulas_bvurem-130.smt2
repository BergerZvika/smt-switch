(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvurem s (bvsub (int_to_pbv k 1) t))) (distinct (int_to_pbv k 0) (bvlshr s t))))
(check-sat)
