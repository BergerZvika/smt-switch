(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvmul t (bvmul t (bvsub s (int_to_pbv k 1)))) (bvmul s (bvmul t (bvshl t (int_to_pbv k 1))))))
(check-sat)
