(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvsub (bvmul t t) (int_to_pbv k 1))) (distinct s (bvmul t (bvshl t (int_to_pbv k 1))))))
(check-sat)
