(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct t (bvmul s (bvneg (int_to_pbv k 0)))) (distinct s (bvmul t (bvnot (int_to_pbv k 0))))))
(check-sat)
