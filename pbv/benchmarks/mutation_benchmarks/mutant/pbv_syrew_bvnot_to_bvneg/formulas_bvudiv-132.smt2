(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (= s (bvshl s (bvudiv (int_to_pbv k 0) t))) (bvule s (bvneg (bvudiv (int_to_pbv k 0) t)))))
(check-sat)
