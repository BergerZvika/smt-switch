(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (bvult s (bvudiv t (bvshl t t))) (bvult s (bvudiv s (bvshl t t))))))
(check-sat)
