(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (distinct t (bvshl t (bvmul s t))) (distinct (_ bv0 k) (bvmul s t)))))
(check-sat)
