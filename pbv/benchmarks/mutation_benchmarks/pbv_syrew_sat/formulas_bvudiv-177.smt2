(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (bvule s (bvand t (bvudiv s t))) (= s (bvand t (bvudiv s t))))))
(check-sat)
