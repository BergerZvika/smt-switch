(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (bvult s (bvmul s (bvand s (int_to_pbv k 1)))) (distinct s s))))
(check-sat)
