(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (distinct s (bvshl s (bvurem t (int_to_pbv k 0)))) (distinct s (bvshl s t)))))
(check-sat)
