(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (= t (bvshl t (bvmul s t))) (= (int_to_pbv k 0) (bvmul s t)))))
(check-sat)
