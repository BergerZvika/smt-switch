(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (distinct s (bvmul t (bvshl (int_to_pbv k 1) t))) (distinct s (bvshl t t)))))
(check-sat)
