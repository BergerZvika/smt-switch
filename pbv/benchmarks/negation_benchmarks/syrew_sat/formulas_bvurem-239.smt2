(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (distinct s (bvurem t (bvlshr (int_to_pbv k 1) s))) (distinct s (bvurem t (bvurem t s))))))
(check-sat)
