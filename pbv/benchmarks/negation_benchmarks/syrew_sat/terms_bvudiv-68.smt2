(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (bvshl s (bvnot (bvudiv (int_to_pbv k 1) t))) (bvand s (bvudiv (int_to_pbv k 0) t)))))
(check-sat)
