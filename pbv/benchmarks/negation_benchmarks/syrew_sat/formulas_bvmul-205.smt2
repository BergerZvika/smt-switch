(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (distinct t (bvshl (bvmul s t) (int_to_pbv k 1))) (bvult (bvlshr s s) t))))
(check-sat)
