(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (distinct s (bvurem s (bvurem t s))) (distinct s (bvshl s (bvurem t s))))))
(check-sat)
