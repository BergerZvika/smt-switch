(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (bvule s (bvshl s (bvurem (int_to_pbv k 1) s))) (bvult s (bvnot s)))))
(check-sat)
