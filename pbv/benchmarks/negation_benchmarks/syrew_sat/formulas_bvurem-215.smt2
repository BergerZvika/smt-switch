(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (distinct s (bvlshr t (bvurem (int_to_pbv k 0) s))) (distinct s t))))
(check-sat)
