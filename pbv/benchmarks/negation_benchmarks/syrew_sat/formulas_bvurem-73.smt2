(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (bvult s (bvnot (bvurem (int_to_pbv k 1) s))) (bvult s (bvnot (int_to_pbv k 1))))))
(check-sat)
