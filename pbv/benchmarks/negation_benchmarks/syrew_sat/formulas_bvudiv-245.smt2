(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (not (distinct (distinct s (bvudiv t (bvudiv (int_to_pbv k 0) t))) (distinct s (bvnot (bvudiv (int_to_pbv k 0) t))))))
(check-sat)
