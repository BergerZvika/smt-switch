(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvshl s (bvmul s t))) (distinct (int_to_pbv k 0) (bvmul s t))))
(check-sat)
