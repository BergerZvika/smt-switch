(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct (int_to_pbv k 0) (bvurem (int_to_pbv k 1) s)) (distinct s (int_to_pbv k 1))))
(check-sat)
