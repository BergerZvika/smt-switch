(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvurem s (bvurem s t))) (distinct (int_to_pbv k 0) (bvurem s t))))
(check-sat)
