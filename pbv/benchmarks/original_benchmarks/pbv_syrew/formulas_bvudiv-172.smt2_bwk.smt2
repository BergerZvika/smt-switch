(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvudiv t (bvlshr s t))) (distinct s (bvudiv t (bvshl s t)))))
(check-sat)
