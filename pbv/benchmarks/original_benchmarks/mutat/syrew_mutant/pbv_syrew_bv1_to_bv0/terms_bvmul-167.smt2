(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvmul t (bvmul t (bvshl s (int_to_pbv k 0)))) (bvmul s (bvmul t (bvshl t (int_to_pbv k 1))))))
(check-sat)
