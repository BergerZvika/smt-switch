(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvand t (bvurem (int_to_pbv k 0) s))) (distinct s (bvand t (bvshl (int_to_pbv k 1) s)))))
(check-sat)
