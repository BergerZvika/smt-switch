(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvudiv s (bvlshr (int_to_pbv k 0) t))) (bvult s (bvnot (bvudiv (int_to_pbv k 0) t)))))
(check-sat)
