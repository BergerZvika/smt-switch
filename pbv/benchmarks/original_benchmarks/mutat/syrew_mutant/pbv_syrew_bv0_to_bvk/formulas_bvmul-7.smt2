(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvult s (bvmul s (bvnot (int_to_pbv k k)))) (bvult s (bvshl s (int_to_pbv k 1)))))
(check-sat)
