(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (bvor (int_to_pbv k 1) (bvmul s (bvnot t))) (bvand s (bvand (int_to_pbv k 1) (bvnot t)))))
(check-sat)
