(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct s (bvor s (bvudiv (int_to_pbv k 0) s))) (distinct s (int_to_pbv k 0))))
(check-sat)
