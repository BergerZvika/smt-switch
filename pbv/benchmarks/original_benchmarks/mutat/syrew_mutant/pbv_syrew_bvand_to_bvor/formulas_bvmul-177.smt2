(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (= s (bvshl (bvor t (int_to_pbv k 1)) t)) (= s (bvmul t (bvshl t t)))))
(check-sat)
