(set-logic ALL)
(declare-const k Int)
(declare-fun s () (_ BitVec k))
(declare-fun t () (_ BitVec k))
(assert (distinct (distinct t (bvor (int_to_pbv k 1) (bvmul s t))) (bvult (bvand s (int_to_pbv k 1)) t)))
(check-sat)
