(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s (_ BitVec 1))
(declare-const t (_ BitVec 1))
(assert (distinct (bvshl (bvadd s s) s) (bvshl (bvshl s s) s)))
(check-sat)
(exit)