(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const m Int)
(declare-const x (_ BitVec k))
(declare-const y (_ BitVec k))
(declare-const z (_ BitVec m))
(assert (= k 1))
(assert (distinct z x (bvsub x y)))
(check-sat)
(exit)