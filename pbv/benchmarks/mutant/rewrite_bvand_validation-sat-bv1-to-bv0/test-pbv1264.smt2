(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s (_ BitVec k))
(declare-const t (_ BitVec k))
(assert (distinct (bvand (_ bv0 k) (bvshl s (bvand s (bvshl s t)))) (bvand s (bvshl (_ bv1 k) (bvand s (bvshl s t))))))
(check-sat)
(exit)