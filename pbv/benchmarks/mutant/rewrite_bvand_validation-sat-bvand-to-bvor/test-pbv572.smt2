(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s (_ BitVec k))
(declare-const t (_ BitVec k))
(assert (distinct (bvshl (bvshl (bvshl (bvor s (_ bv1 k)) t) t) s) (bvshl (bvshl (bvshl (bvand s (_ bv1 k)) s) t) t)))
(check-sat)
(exit)