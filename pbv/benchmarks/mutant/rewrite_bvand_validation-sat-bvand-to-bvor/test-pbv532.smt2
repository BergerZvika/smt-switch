(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s (_ BitVec k))
(declare-const t (_ BitVec k))
(assert (distinct (bvshl (bvshl (bvor s (bvnot (_ bv1 k))) s) t) (bvshl (bvand s (bvshl (bvnot (_ bv1 k)) t)) s)))
(check-sat)
(exit)