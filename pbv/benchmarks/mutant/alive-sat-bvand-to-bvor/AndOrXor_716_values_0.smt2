(set-logic ALL)
(declare-const k Int)
(declare-fun %a () (_ BitVec k))
(declare-fun %d () (_ BitVec k))
(declare-fun %b () (_ BitVec k))
(assert (not (= (and (= (bvor %a %b) %a) (= (bvand %a %d) %a)) (= (bvand %a (bvand %b %d)) %a))))
(check-sat)