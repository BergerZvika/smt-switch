(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s (_ BitVec k))
(declare-const t (_ BitVec k))
(assert (distinct (bvule (bvadd s t) (bvudiv s t)) (bvule t (bvudiv s (bvadd s t)))))
(check-sat)
(exit)