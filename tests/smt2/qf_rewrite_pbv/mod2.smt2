(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s Int)
(declare-const t Int)
(assert (> k 1))
(assert (> s 1))
(assert (= s (mod (* s s) )))
(check-sat)
(get-model)
(exit)
