(set-logic ALL)
(set-option :produce-models true)
(set-option :incremental true)
(declare-const k Int)
(declare-const s (_ BitVec k))
(declare-const t (_ BitVec k))
(assert (distinct (distinct s (bvnot (bvudiv (bvnot s) s))) (bvult s (bvnot (bvudiv (bvneg s) s)))))
(check-sat)
(exit)